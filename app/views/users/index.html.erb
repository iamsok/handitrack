<% "Friend List" %>

<div id="users">
  <% @users.each do |user| %>
    <div class="user">
      <p>
      <%= link_to user.name, user_path(user) %>
      <%= button_to "Add Friend", user_friendships_path(user) %>
      </p>
    </div>
  <% end %>
</div>

