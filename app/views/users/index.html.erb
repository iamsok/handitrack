<% "Friend List" %>

<div id="users">
  <% @users.each do |user| %>
    <div class="user">
      <div class="user_add_button">
        <%= link_to user.name, user_path(user) %>
        <%= link_to "Add Friend", user_friendships_path(user), class: "button tiny" %>
      </div>
    </div>
  <% end %>
</div>

