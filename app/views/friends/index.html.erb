<% title "Friend List" %>

<div id="users">
  <% for user in @users %>
    <div class="user">
      <p>
        <strong><%=h user.username %></strong>
        <%= link_to "Add Friend", friendships_path(:friend_id => user), :method => :post %>
      </p>
    </div>
  <% end %>
</div>
