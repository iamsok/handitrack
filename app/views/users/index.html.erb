<% "Friend List" %>

<div id="users">
  <% @users.each do |user| %>
    <div class="user">
      <p>
        <strong><%=h user.name %></strong>
        <%= button_to "Add Friend", user_friendships_path(user) %>
      </p>
    </div>
  <% end %>
</div>
